# Writer

本页面介绍了如何在LangChain中使用Writer生态系统。
它分为两个部分：安装和设置，以及对具体Writer包装器的引用。

## 安装和设置
- 获取一个Writer API密钥，并将其设置为环境变量（`WRITER_API_KEY`）

## 包装器

### LLM

存在一个Writer LLM包装器，您可以使用它进行访问 
```python
from langchain.llms import Writer
```