# 结构化工具聊天

结构化工具聊天代理能够使用多输入工具。

旧的代理配置将操作输入指定为单个字符串，但是该代理可以使用提供的工具的 `args_schema` 来填充操作输入。

```javascript
import Example from "@snippets/modules/agents/agent_types/structured_chat.mdx"

<Example/>
```
