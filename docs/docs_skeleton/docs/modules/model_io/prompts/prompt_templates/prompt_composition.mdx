# 组合

本笔记本介绍了如何组合多个提示。当您想要重用提示的部分时，这是非常有用的。可以使用PipelinePrompt来完成这一操作。PipelinePrompt包含两个主要部分：

- 最终提示：这是返回的最终提示
- 管道提示：这是一个包含字符串名称和提示模板的元组列表。每个提示模板都将被格式化，然后作为具有相同名称的变量传递给未来的提示模板。

从"@snippets/modules/model_io/prompts/prompt_templates/prompt_composition.mdx"导入Example

<Example/>
