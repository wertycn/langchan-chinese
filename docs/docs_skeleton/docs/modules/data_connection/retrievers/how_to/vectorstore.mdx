# 基于向量存储的检索器

向量存储检索器是一种使用向量存储来检索文档的检索器。它是对向量存储类的轻量级封装，以使其符合检索器接口的要求。
它使用向量存储实现的搜索方法，如相似度搜索和MMR，来查询向量存储中的文本。

一旦构建了向量存储，构建检索器就非常简单。让我们通过一个示例来了解一下。

从"@snippets/modules/data_connection/retrievers/how_to/vectorstore.mdx"导入示例

<Example/>
