# 递归按字符分割

这个文本分割器是通用文本的推荐分割器。它以字符列表作为参数进行参数化。它会按照列表中的字符顺序进行分割，直到分割后的块足够小。默认的字符列表是 `["\n\n", "\n", " ", ""]`。这样做的效果是，尽可能地保持段落（然后是句子，然后是单词）在一起，因为这些通常是语义相关性最强的文本片段。

1. 文本如何分割：按字符列表进行分割
2. 块大小如何测量：按字符数进行测量

从 "@snippets/modules/data_connection/document_transformers/text_splitters/recursive_text_splitter.mdx" 导入 Example

<Example/>
